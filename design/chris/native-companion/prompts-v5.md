# Revision 5 — built-in imagegen prompts

Current outputs: `01-listening-review-v5.png` and `02-processing-recovery-v5.png`. Earlier image versions are superseded. The first board illustrates the left-joystick configuration; actual left/right selection is a design requirement for Pixel, not functionality in this image.

## Listening and review

Use case: ui-mockup
Edit this existing CHRIS design board with only the requested wording changes. Keep the same Open Brush aesthetic, panels, layout, icons, wider font, exact command rows, colors, Stop, Finish recording, Confirm, speech toggle, Close and exterior Off inset.
1. Change board subtitle to "Revision 5 • Simplified wording".
2. LEFT listening panel: replace entire joystick hint block with exactly:
"While CHRIS is open:"
"Click left joystick to toggle recording."
Show those two lines cleanly, wrapping the second only if necessary. This mockup depicts the configuration where the non-drawing controller is left. Do not show brackets, slash, variables or auto-detection code inside the UI. DELETE "No need to hold." completely.
3. RIGHT review panel: rename the microphone button "Re-record" to exactly "Retry", retaining its microphone glyph and secondary visual emphasis.
4. REMOVE the subheading "Apply these in order." completely. Do not replace it with other prose.
5. REMOVE both lines "Reading exact commands..." and "Only Confirm applies commands." completely. Do not replace with any new status or disclaimer inside the panel. Redistribute freed space tastefully; the numbered list and Confirm/Retry remain clear.
6. Change the small exterior caption under listening panel to "Joystick label follows the active hand".
Preserve exact full command rows including 0.3, 0 to 1 scale, #0000FF and Ink. Preserve footer Speech: On opposite Close, and Off inset. No keyboard, Type instead, Edit request, Re-record label, Cancel or new features. Static mockup only; dynamic left/right labeling will be specified in the design notes, not simulated here.

## Processing and recovery

Use case: ui-mockup
Make only a narrow wording revision to this CHRIS four-state design board. Preserve ALL visual styling, Open Brush panels, exact transcript and command values, all state headings, indicators, footer Speech: On controls, Close, Stop, and the existing unknown-outcome Retry control/explanation.
Change board subtitle to "Revision 5 • Simplified wording".
Replace BOTH microphone button labels currently reading "Re-record" with exactly "Retry": one in Finalizing transcript, one in Preparing commands. Keep each microphone glyph and neutral button styling; these are still recording-again actions. The existing cyan Retry in Outcome unknown stays unchanged and retains its circular-arrow icon and reconciliation explanation.
Do not add any new text, buttons, keyboard, edit action or extra feature. Do not change any other wording or positions. Same wide readable body typography, same 2-by-2 board. This is a label-only edit to a design concept, not application implementation.
